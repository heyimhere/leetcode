// LC #136 — Single Number   [Easy]   Pattern: Bit Manipulation
//
// Given a non-empty array of integers nums where every element appears
// exactly TWICE except for one, find that single element.
//
// You must write a solution with linear runtime and constant extra space.
//
// Example 1:
//   Input:  nums = [2,2,1]
//   Output: 1
// Example 2:
//   Input:  nums = [4,1,2,1,2]
//   Output: 4
// Example 3:
//   Input:  nums = [1]
//   Output: 1
//
// Constraints:
//   1 <= nums.length <= 3 * 10^4
//   -3 * 10^4 <= nums[i] <= 3 * 10^4
//   Each element appears twice except for one element which appears once.

const singleNumber = (nums) => {

};

console.log(singleNumber([2, 2, 1]));        // expected: 1
console.log(singleNumber([4, 1, 2, 1, 2]));  // expected: 4
console.log(singleNumber([1]));               // expected: 1
