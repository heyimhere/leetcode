const number = 121;
function isPalindrome(x) {
  if(x < 0) return false;
  let rev = 0;
  for(let i = x; i >= 1; i = Math.floor(i/10)) {
    rev = rev * 10 + i % 10;
  }
  return rev === x;
}

// practice 1
function isPalindrome(x) {
  if(x < 0) return false;
  let rev = 0;
  for(let i = x; i >= 1; i = Math.floor(i/10)) {
    rev = rev * 10 + i % 10;
  }
  return rev === x;
}

// practice 2
function isPalindrome(x) {
  if(x < 0) return false;
  let rev = 0;
  for(let i = x; i >= 1; i = Math.floor(i/10)) {
    rev = rev * 10 + i % 10;
  }
  return rev === x;
}

// practice 3
function isPalindrome(x) {
  if(x < 0) return false;
  let rev = 0;
  for(let i = x; i >= 1; i = Math.floor(i/10)) {
    rev = rev * 10 + i % 10;
  }
  return rev === x;
}

// practice 4
function isPalindrome(x) {
  if(x < 0) return false;
  let rev = 0;
  for(let i = x; i >= 1; i = Math.floor(i/10)) {
    rev = rev * 10 + i % 10;
  }
  return rev === x;
}

// practice 5
function isPalindrome(x) {
  if(x < 0) return false;
  let rev = 0;
  for(let i = x; i >= 1; i = Math.floor(i/10)) {
    rev = rev * 10 + i % 10;
  }
  return rev === x;
}

// practice 6
function isPalindrome(x) {
  if(x < 0) return false;
  let rev = 0;
  for(let i = x; i >= 1; i = Math.floor(i/10)) {
    rev = rev * 10 + i % 10;
  }
  return rev === x;
}

// practice 7
function isPalindrome(x) {
  if(x < 0) return false;
  let rev = 0;
  for(let i = x; i >= 1; i = Math.floor(i/10)) {
    rev = rev * 10 + i % 10;
  }
  return rev === x;
}

// practice 8
function isPalindrome(x) {
  if(x < 0) return false;
  let rev = 0;
  for(let i = x; i >= 1; i = Math.floor(i/10)) {
    rev = rev * 10 + i % 10;
  }
  return rev === x;
}

// practice 9
function isPalindrome(x) {
  if(x < 0) return false;
  let rev = 0;
  for(let i = x; i >= 1; i = Math.floor(i/10)) {
    rev = rev * 10 + i % 10;
  }
  return rev === x;
}

// practice 10
function isPalindrome(x) {
  if(x < 0) return false;
  let rev = 0;
  for(let i = x; i >= 1; i = Math.floor(i/10)) {
    rev = rev * 10 + i % 10;
  }
  return rev === x;
}

function isPalindrome(x) {
  if(x < 0) return false;
  let rev = 0;
  for(let i = x; i >= 1; i = Math.floor(i/10)) {
    rev = rev * 10 + i % 10;
  }
  return rev === x;
}

function isPalindrome(x) {
  if(x < 0) return false;
  let rev = 0;
  for(let i = x; i >= 1; i = Math.floor(i/10)) {
    rev = rev * 10 + i % 10;
  }
  return rev === x;
}

function isPalindrome(x) {
  if(x < 0) return false;
  let rev = 0;
  for(let i = x; i >= 1; Math.floor(i/10)) {
    rev = rev * 10 + i % 10;
  }
  return rev === x;
}

function isPalindrome(x) {
  if(x < 0) return false;
  let rev = 0;
  for(let i = x; i >= 1; Math.floor(i/10)) {
    rev = rev * 10 + i % 10;
  }
  return rev === x;
}

function isPalindrome(x) {
  if(x < 0) return false;
  let rev = 0;
  for(let i = x; i >= 1; i = Math.floor(i/10)) {
    rev = rev * 10 + i % 10;
  }
  return rev === x;
}

function isPalindrome(x) {
  if(x < 0) return false;
  let rev = 0;
  for(let i = x; i >= 1; i = Math.floor(i/10)) {
    rev = rev * 10 + i % 10;
  }
  return rev === x;
}

// coding interview sample

console.log('is Palindrome number', isPalindrome(number));
