// TODO: implement
// LC #389 — Find the Difference
//
// You're given two strings s and t. t is generated by shuffling s and ADDING
// exactly ONE extra lowercase letter. Return that extra letter.
//
// Example:
//   s = "abcd", t = "abcde"  → 'e'
//   s = "",     t = "y"      → 'y'
//   s = "a",    t = "aa"     → 'a'
//
// Approach 1 (XOR of char codes — elegant):
//   XOR all char codes of s and all char codes of t together. Every paired
//   char cancels (x ^ x = 0); the lone extra char remains.
//   return String.fromCharCode(xor).
//
//   Why XOR works: same property as LC #136 Single Number — pairs cancel,
//   loner survives. O(1) space, O(n) time, no hashmap allocation.
//
// Approach 2 (count map):
//   Build counts for t. Decrement for each char in s. The char left with
//   count > 0 at the end is the answer.
//
// Approach 3 (sum of char codes):
//   sum(t) - sum(s) === code of the extra char. Same idea as XOR, but uses
//   numeric overflow assumption (safe in JS for normal strings).
//
// Time:  O(n)
// Space: O(1)
//
// Edge Cases:
//   - s is empty, t has one char    → return t[0]
//   - extra char is a duplicate     → still works (XOR / counts handle it)
//   - very long strings             → linear, no issue

/**
 * @param {string} s
 * @param {string} t
 * @returns {string}
 */
const findTheDifference = (s, t) => {
  // your code here
};

console.log('=== LC #389 Find the Difference ===\n');

console.log('Test 1:');
console.log(findTheDifference('abcd', 'abcde'));
// Expected: 'e'

console.log('\nTest 2 — empty s:');
console.log(findTheDifference('', 'y'));
// Expected: 'y'

console.log('\nTest 3 — extra is a duplicate:');
console.log(findTheDifference('a', 'aa'));
// Expected: 'a'

console.log('\nTest 4:');
console.log(findTheDifference('abc', 'cabz'));
// Expected: 'z'
